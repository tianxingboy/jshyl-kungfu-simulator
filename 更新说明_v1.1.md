# 金书红颜录5.60 武功学习规划模拟器

## v1.1 更新说明

### 性能优化（重点）
- **读档速度提升约80倍**：打包后读档从5.5秒降至0.1秒以内
  - 根因：windowed程序调用控制台子进程unpack.exe时，创建/销毁控制台窗口耗时5秒
  - 修复：使用 `CREATE_NO_WINDOW` 标志调用 unpack.exe，不创建控制台
- **unpack.exe 只调用一次**：解包后复用 R.grp 分别解析秘籍和人物（原调用2次）
- **人物解析两阶段筛选**：2480人先只解析名字快速筛选，仅约238个可加入队友需完整解析
- **秘籍库缓存**：首次解析后保存为本地JSON，后续启动/读档直接加载，跳过解包
- **解包结果缓存**：同一存档未修改时复用已解包的 R.grp

### 功能修复
- **内属调和逻辑修正**：调和是最终态，只能从阴/阳转调和，不能反向；删除武功后恢复未学习前状态
- **轻功加点成本修正**：从3点根骨/属性改为2点根骨/属性（与规则书一致）
- **剩余根骨值修正**：直接从存档读取，不再通过总点数-已分配计算
- **人物属性索引修正**：通过逐字节分析存档原始数据，修正所有属性字段的索引偏移
- **初始存档加载修复**：打包后初始存档在只读目录，改用临时目录解包

### 使用说明
- 双击 `金书武功模拟器.exe` 直接运行，无需安装Python环境
- 首次启动自动加载初始存档的秘籍库和队友列表
- 点击「读取存档」可加载自己的 `jshyl_save1.zx5`
- 支持武功学习/洗武功规划、属性加点、等级模拟、明玉丹格子计算
- 支持人物洗武功模版导入
